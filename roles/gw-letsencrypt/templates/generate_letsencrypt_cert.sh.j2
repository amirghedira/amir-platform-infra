#!/usr/bin/env bash

DOMAIN="$1"

echo "{{ letsencrypt_choice }}"|{{ certbot_bin }} --config "{{ letsencrypt_root_path }}/configs/${DOMAIN}.conf" --non-interactive --agree-tos certonly > "{{ node_home }}/letsencrypt-${DOMAIN}.log" 2>&1

